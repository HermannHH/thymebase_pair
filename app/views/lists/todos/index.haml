%h1= @list.name

= render partial: 'form', locals: { list: @list, todo: @todo }

%table
  %thead
    %tr
      %th Description
      %th Status
      %th
      %th

  %tbody
    - @todos.each do |todo|
      %tr
        %td= todo.description
        %td= todo.status.humanize
        -# %td= link_to 'Show', list
        -# %td= link_to 'Edit', edit_list_path(list)
        -# %td= link_to 'Destroy', list, :method => :delete, :data => { :confirm => 'Are you sure?' }

%br

= link_to 'Back to lists', lists_path
